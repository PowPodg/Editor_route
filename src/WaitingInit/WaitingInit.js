import React from 'react';
import cssClasses from './WaitingInit.css'

const WaitingInit = props => (
    <div className={ cssClasses.center }>
        <div className={ cssClasses.WaitingInit }>
            <div /><div />
        </div>
    </div>
)

export default WaitingInit
